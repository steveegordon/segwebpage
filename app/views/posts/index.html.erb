<h1>Listing articles</h1>
 

 
  <% @posts.each do |post| %>
  <div class="post">
      <h3 class="date"><%= l(post.created_at, :format => :basic) %></h3>
      <h2 class="title"><%= post.title %></h2>
      <p class="text"><%= post.text %></p>
  </div>
  <% end %>
